import 'package:flutter/material.dart';

/// Defines the color palette for the Evently App UI Kit.
abstract class AppColors {
  /// Evently Primary Coral Red color (#FF4040).
  static const Color primary = Color(0xFFFF4040);

  /// Evently Primary Red variant (#E63939).
  static const Color primaryDark = Color(0xFFE63939);

  /// Evently Light Coral background tint (#FFF0F0).
  static const Color primaryLight = Color(0xFFFFF0F0);

  /// Pure Black (#000000).
  static const Color black = Color(0xFF000000);

  /// Pure White (#FFFFFF).
  static const Color white = Color(0xFFFFFFFF);

  /// Transparent (0x00000000).
  static const Color transparent = Color(0x00000000);

  /// Dark Navy / Primary Text color (#1E293B).
  static const Color textPrimary = Color(0xFF1E293B);

  /// Neutral Gray / Secondary Text color (#64748B).
  static const Color textSecondary = Color(0xFF64748B);

  /// Muted Text / Placeholder color (#94A3B8).
  static const Color textMuted = Color(0xFF94A3B8);

  /// Default Light Background (#F8F9FA).
  static const Color background = Color(0xFFF8F9FA);

  /// Dark Mode Background (#121212).
  static const Color backgroundDark = Color(0xFF121212);

  /// Card Surface color (#FFFFFF).
  static const Color surface = Color(0xFFFFFFFF);

  /// Dark Card Surface color (#1E1E1E).
  static const Color surfaceDark = Color(0xFF1E1E1E);

  /// Light Gray Border / Outline (#E2E8F0).
  static const Color borderOutline = Color(0xFFE2E8F0);

  /// Dark Mode Border / Outline (#334155).
  static const Color borderOutlineDark = Color(0xFF334155);

  /// Input Background color (#F1F5F9).
  static const Color inputBackground = Color(0xFFF1F5F9);

  /// Success Green color (#10B981).
  static const Color success = Color(0xFF10B981);

  /// Warning Amber/Yellow color (#F59E0B).
  static const Color warning = Color(0xFFF59E0B);

  /// Error Red color (#EF4444).
  static const Color error = Color(0xFFEF4444);

  /// Info Blue color (#3B82F6).
  static const Color info = Color(0xFF3B82F6);

  /// Generic Grey.
  static const Color grey = Colors.grey;

  /// Bright / Light Grey (#E2E8F0).
  static const Color brightGrey = Color(0xFFE2E8F0);

  /// Dark Grey (#475569).
  static const Color darkGrey = Color(0xFF475569);
}
